import * as React from 'react';

interface IPercentProps {
}

const Percent
: React.FunctionComponent<IPercentProps> = (props) => {
    return (
        <div className='container'>
            <h4>Max 100%</h4>
        </div>
    );
};

export default Percent
;
